<<<<<<< SEARCH
    it('should allow token generation for owners', async () => {
      const app = setupApp('owner');
      const res = await app.request('/tracker/generate', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(validPayload),
      });
      expect(res.status).toBe(200);
    });
  });

  describe('DELETE /tracker/:id', () => {
=======
    it('should allow token generation for owners', async () => {
      const app = setupApp('owner');
      const res = await app.request('/tracker/generate', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(validPayload),
      });
      expect(res.status).toBe(200);
    });

    it('should handle errors and return 500', async () => {
      const app = setupApp('admin');

      // Override the prisma create mock to throw an error
      app.use('*', async (c, next) => {
        const dollars = c.get('$');
        dollars.prisma.tracker.create = vi.fn().mockRejectedValue(new Error('Database error'));
        c.set('$', dollars);
        await next();
      });

      // Suppress console.error for this test
      const consoleSpy = vi.spyOn(console, 'error').mockImplementation(() => {});

      const res = await app.request('/tracker/generate', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(validPayload),
      });

      expect(res.status).toBe(500);
      const data = await res.json();
      expect(data).toHaveProperty('error', 'Failed to generate token');

      consoleSpy.mockRestore();
    });
  });

  describe('DELETE /tracker/:id', () => {
>>>>>>> REPLACE
